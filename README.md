# Roper Technologies (ROP) — Buy-Side Evidence Deck (dev)

Single-page evidence deck. Every number is scripted into `data/deck_data.json`
by the source repo's `scripts/build_deck_data.py`; the HTML only renders it.

By JAOD & (mainly) Claude. Deploy target: **dev**.
